#' Subsampled dataset of wire and blade cuts
#'
#' This dataset is a subsampled version of the original `x3p` wire and blade cuts data. The subsampling is performed at a factor of 10.
#'
#' @format A list of `x3p` objects, each representing a surface scan. The list includes:
#' * T1AW-LO-R1: An `x3p` object representing the first surface scan.
#' * T1AS-R1: An `x3p` object representing the second surface scan.
"x3p_subsamples"
